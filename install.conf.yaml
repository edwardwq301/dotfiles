- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.clang-format: clang/clang-format
    ~/.tmux.conf: tmux.conf

- shell:
    - [git submodule update --init --recursive, Installing submodules]
