- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.clang-format: clang/clang-format
    ~/.tmux.conf: tmux.conf
    ~/.config/nvim/:
        path: nvim/**
- shell:
    - [git submodule update --init --recursive, Installing submodules]
